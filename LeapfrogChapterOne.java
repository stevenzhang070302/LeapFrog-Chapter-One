import java.util.Scanner;
public class LeapfrogChapterOne
{

   public static void main(String[] args)
   {
      /*Scanner keyboard = new Scanner(System.in);
      //System.out.print("Enter the T-value: ");
      int T = keyboard.nextInt();
      int countA = 0;
      int countB = 0;
      int countPeriod = 0;
      
      if(T >= 0 && T <= 500)
      {
         for(int i = 1; i <= T+1; i++)
         {
            String N = keyboard.nextLine();
            if((N.length() >= 2) && (N.length() <= 5000))
            {
               for(int a = 0; a < N.length(); a++)
               {
                  if(N.charAt(a) == 'A')
                     ++countA;
                  if(N.charAt(a) == 'B')
                     ++countB;
                  if(N.charAt(a) == '.')
                     ++countPeriod;
               }
            //Debugging Purposes
            System.out.println("A: " + countA);
            System.out.println("B: " + countB);
            System.out.println("Period: " + countPeriod);
            
            if((countA == 1) && (countPeriod != 0) && (countB != 0) && (countB >= countPeriod))
            {   
               System.out.print("Case #");
               System.out.print(i-1);
               System.out.println(": Y");
            }
            else
            {
               System.out.print("Case #");
               System.out.print(i-1);
               System.out.println(": N");
            }   
            }
            countA = 0;
            countB = 0;
            countPeriod = 0;
         } 
         
         
         
         
         
      }
      
      else
      {
         System.out.println("Error encountered, because the T-value is not within the specified constraints.");
      
      } */
      System.out.println("Gowrav has big Gay");
      System.out.println("\n" + "New text");
   
   }
   
}